import { HoldingsPage } from "@/components/holdings/HoldingsPage";

// ---------------------------------------------------------------------------
// /holdings — Server Component stub; mounts HoldingsPage (Client Component)
// ---------------------------------------------------------------------------

export const metadata = {
  title: "Holdings — FINTrack",
};

export default function HoldingsRoute() {
  return <HoldingsPage />;
}
